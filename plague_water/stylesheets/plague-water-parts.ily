#(set-default-paper-size "11x17" 'portrait)
#(set-global-staff-size 14)

\paper {
    system-system-spacing = #'(
        (basic-distance . 0)
        (padding . 4)
        (stretchability . 100)
        )
}
